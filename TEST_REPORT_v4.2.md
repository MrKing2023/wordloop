# WordLoop v4.2 测试报告

## 数据检查

- CET-6 基础词条：5407；
- CET-6 增强卡：100；
- 考研核心基础词条：4112；
- 考研增强卡：100；
- 考研卡唯一 ID：通过；
- 考研卡唯一目标词：通过；
- 考研卡唯一主例句：通过；
- 每张卡均归属 `libraryId=kaoyan`：通过；
- 每个目标词均存在于考研基础词库：通过；
- 填空前缀 + 答案 + 后缀可还原原句：通过；
- 必填释义、用法、搭配、近义区别、词族和扩展例句：通过；
- GitHub 项目中的标准路径文件完整；CloudBase 专用部署包另行提供根目录备用文件。

## 代码检查

- `app.js`：Node 语法检查通过；
- `enhancements.js`：Node 语法检查通过；
- `plan_v3.js`：Node 语法检查通过；
- `lexicon_v4.js`：Node 语法检查通过；
- Python 生成和验证脚本：编译通过；
- 本地 HTTP 对 `data/cet6/` 和 `data/kaoyan/` 标准 JSON 路径均返回 200。

## 浏览器逻辑检查

使用无头 Chromium、模拟静态资源响应和浏览器本地存储完成以下流程：

- 打开词库中心并显示考研词库：通过；
- 打开考研详情并显示 4112/100：通过；
- 点击“启用这个词库”：通过；
- 本地写入 100 张考研卡：通过；
- 自动生成 100 词独立学习计划：通过；
- 考研词典搜索 `address` 返回 1 条：通过；
- 进入第 1 天页面：通过；
- 切换到 CET-6 详情仍显示 5407：通过；
- 浏览器控制台和页面异常：0。

## v4.2.1 本地复测

- 使用仓库根目录和 `public/` 网站目录在端口 8042 启动：通过；
- 考研详情显示 4112/100：通过；
- 点击启用后加载 100 张考研卡并生成 5 天计划：通过；
- 考研基础词库浏览器显示 4112 个词条：通过；
- 浏览器控制台错误：0；
- `file://` 模式现在显示明确的本地服务器启动说明，不再只显示 `Failed to fetch`。
